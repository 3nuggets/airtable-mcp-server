# Copy to `.dev.vars` for local `wrangler dev`. Never commit `.dev.vars`.
# In production these are set with `npx wrangler secret put <NAME>`.

# From your Airtable OAuth integration (https://airtable.com/create/oauth)
AIRTABLE_CLIENT_ID=
AIRTABLE_CLIENT_SECRET=

# Seals the stateless tokens this server issues. Any long random string.
# Generate one with:  openssl rand -hex 32
# Rotating this invalidates every issued token (i.e. logs everyone out).
TOKEN_SEALING_KEY=
